---
import siteData from '../data/site-data.json'
const { navLinks } = siteData
---

<nav>
  <ul>
    {
      navLinks.map(({ title, path }) => (
        <li>
          <a href={path}>{title}</a>
        </li>
      ))
    }
  </ul>
</nav>
